{{- if gt (len .Messages) 0 -}}
using Google.Protobuf;

namespace {{ .CsharpNamespace }}
{
    {{ range .Messages }}
    public sealed partial class {{ .Name }} : IMessage<{{ .Name }}>
    {

    }
    {{ end }}
}
{{- end -}}